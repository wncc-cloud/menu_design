// Placeholder — app requires Firebase initialization which cannot run in widget tests.
// Integration tests live in integration_test/.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
